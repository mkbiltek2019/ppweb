angular.module("functions", []).factory("functions", ["$rootScope", "$window", "$translate", function ($rootScope, $window, $translate) { return { isNullOrEmpty: function (x) { var res = false; if (x === "" || x == undefined || x == null) { res = true } return res }, getDateDiff: function (x) { var today = new Date; var date2 = today; var date1 = new Date(x); var diffDays = Math.abs(parseInt((date2 - date1) / (1e3 * 60 * 60 * 24))); return diffDays } } }]);